<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@android:color/white" />  <!-- خلفية بيضاء -->
    <stroke
        android:width="1dp"
        android:color="@color/white" />  <!-- حدود رمادية فاتحة -->
    <corners android:radius="16dp" />  <!-- نفس نصف القطر للكارد -->
</shape>
